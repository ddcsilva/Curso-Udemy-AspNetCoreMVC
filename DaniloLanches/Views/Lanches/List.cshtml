@model LancheListViewModel

<div>
    <h2 class="mb-4">@Model.CategoriaAtual</h2>
</div>

<div class="container">
    <div class="row">
        @foreach (var lanche in Model.Lanches)
        {
            <div class="col-sm-12 col-md-6 col-lg-4 mb-4">
                <div class="card h-100">
                    <img src="@lanche.ImagemUrl" class="card-img-top" alt="@lanche.Nome">
                    <div class="card-body">
                        <h4 class="card-title">@lanche.Nome</h4>
                        <p class="card-text">@lanche.DescricaoCurta</p>
                        <h5 class="card-price">Preço: @lanche.Preco.ToString("C")</h5>
                    </div>
                    <div class="card-footer">
                        <a href="#" class="btn btn-primary">Comprar</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
